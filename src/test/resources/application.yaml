spring:
  cache:
    type: none
server:
  port: 8812
  servlet:
    context-path: /syfoperson

no.nav.security.jwt:
  issuer:
    veilederaad:
      discoveryurl: http://metadata
      accepted_audience: aud-localhost
      cookie_name: localhost-idtoken

azure:
  app:
    client:
      id: "1345678"
      secret: "secret"
  openid:
    config:
      token:
        endpoint: "https://login.microsoftonline.com/id/oauth2/v2.0/token"

syfotilgangskontroll.client.id: "syfotilgangskontrollId"

isproxy.client.id: "dev-fss.teamsykefravr.isproxy"
isproxy.url: "https://isproxy.dev.intern.nav.no"
tilgangskontrollapi.url: "https://tilgangskontroll.nais.no"
pdl.client.id: "dev-fss.pdl.pdl-api"
pdl.url: "https://pdl.nais.no"
